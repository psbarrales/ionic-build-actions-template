app_identifier("io.ionic.starter") # The bundle identifier of your app
# google_play_key_file("path/to/your/service-account-key.json") # Path to the json secret file - Follow https://docs.fastlane.tools/actions/supply/#setup to get one
package_name("io.ionic.starter") # e.g. com.krausefx.app

# Uncomment and set the following if you have a Google Play Console service account key
# json_key_file("path/to/your/service-account-key.json")

# You can also specify the key directly as an environment variable
# This is recommended for CI/CD environments
# Set GOOGLE_PLAY_JSON_KEY_DATA environment variable with the content of your service account key file