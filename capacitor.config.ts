import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.psbarrales.ionicapp',
  appName: 'Ionic App Build',
  webDir: 'dist',
};

export default config;
